import styles from "./FlowPanel.module.scss";

export const FlowPanel = () => {
    return (
        <div className={styles.container}>
            <h2 className={styles.title}>Flow Panel</h2>
        </div>
    );
};
